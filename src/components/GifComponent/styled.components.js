import styled from "styled-components"

export const GifSection = styled.div`
  video {
    object-fit: cover;
    object-position: center;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 1;
    min-height: 50px;
  }
`
